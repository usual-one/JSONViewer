#ifndef COLOR_SCHEME_QCIVIC_H
#define COLOR_SCHEME_QCIVIC_H

// colors from
// https://github.com/foxoman/qcivic

#include <QColor>

// background
const QColor EDITOR_DEFAULT_BACKGROUND_COLOR = QColor(255, 255, 255);

const QColor EDITOR_SELECTED_BACKGROUND_COLOR = QColor(80, 116, 125);

const QColor EDITOR_MISTAKE_BACKGROUND_COLOR = QColor(146, 131, 116);

// text
const QColor EDITOR_MISTAKE_TEXT_COLOR = EDITOR_DEFAULT_BACKGROUND_COLOR;

const QColor EDITOR_DEFAULT_TEXT_COLOR = QColor(37, 36, 46);

const QColor EDITOR_NUMBER_TEXT_COLOR = QColor(92, 124, 140);

const QColor EDITOR_STRING_TEXT_COLOR = QColor(224, 0, 28);

const QColor EDITOR_KEY_TEXT_COLOR = QColor(0, 149, 142);

const QColor EDITOR_NULL_TEXT_COLOR = QColor(123, 91, 177);

const QColor EDITOR_BOOLEAN_TEXT_COLOR = QColor(123, 91, 177);

#endif // COLOR_SCHEME_QCIVIC_H

#ifndef COLOR_SCHEME_MONOKAI_H
#define COLOR_SCHEME_MONOKAI_H

// colors from
// https://github.com/SuperPaintman/vscode-monokai-extended

#include <QColor>

const QColor EDITOR_BACKGROUND_COLOR = QColor(39, 40, 34);

const QColor EDITOR_SELECTED_BACKGROUND_COLOR = QColor(55, 57, 46);

const QColor EDITOR_MISTAKE_BACKGROUND_COLOR = QColor(74, 63, 16);

const QColor EDITOR_DEFAULT_TEXT_COLOR = QColor(248, 248, 242);

const QColor EDITOR_NUMBER_TEXT_COLOR = QColor(174, 129, 255);

const QColor EDITOR_STRING_TEXT_COLOR = QColor(230, 219, 116);

const QColor EDITOR_KEY_TEXT_COLOR = QColor(249, 38, 114);

const QColor EDITOR_NULL_TEXT_COLOR = QColor(102, 217, 239);

const QColor EDITOR_BOOLEAN_TEXT_COLOR = QColor(102, 217, 239);

#endif // COLOR_SCHEME_MONOKAI_H

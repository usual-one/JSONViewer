#ifndef COLOR_SCHEME_GRUVBOX_DARK_H
#define COLOR_SCHEME_GRUVBOX_DARK_H

// colors from
// https://github.com/morhetz/gruvbox

#include <QColor>

const QColor EDITOR_BACKGROUND_COLOR = QColor(40, 40, 40);

const QColor EDITOR_SELECTED_BACKGROUND_COLOR = QColor(60, 56, 54);

const QColor EDITOR_MISTAKE_BACKGROUND_COLOR = QColor(146, 131, 116);

const QColor EDITOR_DEFAULT_TEXT_COLOR = QColor(235, 219, 178);

const QColor EDITOR_NUMBER_TEXT_COLOR = QColor(177, 98, 134);

const QColor EDITOR_STRING_TEXT_COLOR = QColor(215, 153, 33);

const QColor EDITOR_KEY_TEXT_COLOR = QColor(152, 151, 26);

const QColor EDITOR_NULL_TEXT_COLOR = QColor(204, 36, 29);

const QColor EDITOR_BOOLEAN_TEXT_COLOR = QColor(204, 36, 29);

#endif // COLOR_SCHEME_GRUVBOX_DARK_H
